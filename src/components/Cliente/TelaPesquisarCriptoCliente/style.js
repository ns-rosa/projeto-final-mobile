import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  textPesquisar: { textAlign: "center", color: '#656565', marginTop:10}
});


export default styles